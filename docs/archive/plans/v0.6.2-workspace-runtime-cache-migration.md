---
title: Workspace Runtime 聚合与缓存所有权迁移
status: archived
owner: cross-system
target: v0.6.2
scope: workspace-runtime-cache-ownership-migration
archived_at: 2026-09-13
implemented_by: 提交 4ef264a、ad46287、6693b42、984cb21、21df5d1、aad2ad4（分支 fix/mtp-cache-scope-revalidation）
superseded_by: docs/architecture/workspace.md、docs/system/composition.md、docs/system/runtime-and-bus.md、docs/alice/README.md、docs/alice/pending-atom.md、docs/contracts/mtp.md、docs/governance/security/identity-and-execution-safety.md、docs/governance/reliability/durability-and-recovery.md、docs/architecture/decisions/0004-workspace-derived-cache-partitioning.md
code_paths:
  - src/hivememory/system/assembler.py
  - src/hivememory/system/runtime/workspace/
  - src/hivememory/alice/runtime/core.py
  - src/hivememory/alice/runtime/profile_resolver.py
  - src/hivememory/agent_runtime/aliases/resolver.py
  - src/hivememory/agent_runtime/pending_atom/
  - src/hivememory/agent_runtime/mtp/runtime.py
  - src/hivememory/alice/application/agent_run_service.py
related_docs:
  - docs/architecture/workspace.md
  - docs/architecture/boundaries.md
  - docs/system/composition.md
  - docs/alice/README.md
  - docs/alice/agent-runtime.md
  - docs/alice/pending-atom.md
  - docs/contracts/mtp.md
  - docs/governance/security/identity-and-execution-safety.md
  - docs/governance/reliability/durability-and-recovery.md
last_reviewed: 2026-09-13
---

# Workspace Runtime 聚合与缓存所有权迁移

> **已归档（2026-09-13）**：WRT-0～WRT-5 全部完成并通过验收与代码评审。当前事实见上方 `superseded_by` 列出的文档；本文仅保留目标设计、实施记录（含 WRT-4 补充调整的 cache 位置迁移裁决）与验收证据，不再描述当前系统。

## 1. 背景与问题

v0.6.2 W0 建立 Workspace 后，MemoryAtom、Topic、Artifact 和 WorkspaceAsset 已经具备 Workspace ownership。W1 附件链路又增加了一个由 System 装配、跨 Patchouli/Alice 使用的 Workspace-owned working set。与此同时，Agent profile cache 和 Koakuma atom cache 仍由 AliceRuntime 直接创建，PendingAtomRuntime 也与它们处在同一组 Agent runtime 依赖中。

这张对象图能够工作，但没有清楚区分三类状态：Workspace 资源的派生缓存、Agent 执行期间的可变状态机，以及仅负责组合这些资源的进程级运行时。尤其是两个 cache 当前没有完整的 Workspace 维度：Agent profile cache 按 `ActorIdentity + alias` 缓存，atom cache 的 alias 索引是全局的。Workspace 之间存在同名 alias 或不同 profile 时，缓存会出现错误命中、无效覆盖或不必要的冷查询。

本计划建立一个进程级唯一的 `WorkspaceRuntime` 聚合，迁移两个 Workspace-oriented cache 的持有关系，并重新定义它们的 key、命中校验和生命周期。`PendingAtomRuntime` 是否成为 Workspace 基础设施保留为独立议题，本计划不将其迁移。

## 2. 目标

1. 创建一个由 System 组合根装配的单实例 `WorkspaceRuntime`，用于聚合 Workspace-owned working set 及其派生 cache。
2. 将 `AgentProfileCache` 与 `KoakumaAtomCache` 的创建和持有关系从 AliceRuntime 解耦，改由 WorkspaceRuntime 提供明确的 cache port。
3. 使两个 cache 的索引能够区分 Workspace，同时继续在最终 resolver/owner 边界执行完整 `IdentityScope` 校验。
4. 保持一个 WorkspaceRuntime 实例，不按 Workspace 复制 Runtime，不读取或保存 `current_workspace`。
5. 保持附件 Store、cache、Alice、Patchouli 的关闭顺序和各自的生命周期语义，不用一个统一 `clear()` 隐藏跨系统 drain 依赖。
6. 通过跨 Workspace、跨 Actor、同 alias 和 settlement 回填测试，证明迁移后不会产生缓存串扰。

## 3. 非目标

- 不把 `PendingAtomRuntime` 从 Alice/Agent Runtime 迁移到 WorkspaceRuntime。
- 不改变 PendingAtom 状态机、alias/intent settlement 协议、materialize task 或事件名称。
- 不把 WorkspaceRuntime 变成业务控制器、授权服务、队列、调度器或新的资源事实源。
- 不为每个 Workspace 建立独立的 Alice、Patchouli、Agent loop、EventBus 或 cache 实例。
- 不承诺 cache 跨进程持久化、重启恢复、跨进程失效或 Profile 版本订阅。
- 不在本计划中修改 Memory、Topic、Artifact 或 WorkspaceAsset 的持久化模型。

## 4. 当前基线

当前 AliceRuntime 直接创建以下对象：

```text
AliceRuntime
├── AgentProfileResolver ── AgentProfileCache
├── PendingAtomRuntime
├── KoakumaAtomCache
├── RuntimeAliasResolver ── PendingAtomRuntime + KoakumaAtomCache
└── AgentRuntime ── PendingAtomRuntime
```

其中：

- `AgentProfileResolver` 从 Patchouli 读取 Workspace-owned Agent Profile，但 cache key 不包含 Workspace，且 cache hit 不重新执行 Profile owner/policy 校验；
- `KoakumaAtomCache` 维护全局 `alias -> UUID` 与 `UUID -> MemoryAtom` 索引，resolver 在 L1 命中后重新检查 Workspace ownership 与 actor policy；
- `PendingAtomRuntime` 保存可变 PendingAtom、状态迁移、run/frame 认领、settlement 和回收，是 Alice 执行链的状态真相；
- W0/W1 的当前文档把 cache、queue、registry、scheduler、runtime 和 EventBus 定义为进程级共享基础设施。本计划只对两个明确需要 Workspace-aware 索引的派生 cache 建立例外，并记录该设计变化。

## 5. 目标设计

```text
SystemAssembler
└── WorkspaceRuntime                 # 进程级唯一聚合
    ├── WorkspaceAssetStore          # 既有 System-owned working set
    ├── AgentProfileCache            # Workspace + Actor + alias
    └── KoakumaAtomCache             # Workspace + alias / UUID

AliceRuntime
├── AgentProfileResolver ─────────── WorkspaceRuntime.profile_cache_port
├── RuntimeAliasResolver ─────────── WorkspaceRuntime.atom_cache_port
├── PendingAtomRuntime               # 继续由 Alice 持有
└── AgentRuntime ──────────────────── PendingAtomRuntime
```

`WorkspaceRuntime` 只负责组合、访问入口和分阶段清理；每个子组件仍拥有自己的数据结构、容量、失效和错误语义。WorkspaceRuntime 不接收一个可变的当前 Workspace，也不根据 scope 创建子 Runtime。

### 5.1 Agent profile cache

第一版采用完整授权坐标作为 key：

```text
(WorkspaceIdentity, ActorIdentity, agent_alias) -> AgentProfile
```

这样可以表达同一 Actor 在不同 Workspace 使用同名但不同内容的 profile，也能避免私有/团队 profile 在不同 Actor 之间复用。`default`、`omni_doll` 和空 alias 继续直接返回内置 `OMNI_DOLL_PROFILE`。

后续若需要减少按 Actor 的重复缓存，可以改为缓存带来源元数据的 MemoryAtom，并在命中时重新执行 owner/policy 校验；这不属于本次迁移的必需出口。

### 5.2 Atom cache

alias 索引改为 Workspace-aware：

```text
(WorkspaceIdentity, alias) -> UUID
UUID -> MemoryAtom
```

UUID 仍然是全局资源 ID，但所有对外 alias 读取都必须带 WorkspaceIdentity。resolver 命中 UUID 后继续检查 Memory ownership 和 actor policy；缓存 key 不能替代授权。写入时若同一 `(WorkspaceIdentity, alias)` 指向不同 UUID，必须按既有失效/替换语义处理，不能留下两个相互矛盾的 alias 反查结果。

### 5.3 Resolver 与边界

`RuntimeAliasResolver` 继续留在 Agent runtime 侧，因为它编排 L0 Pending、L1 Atom cache 和 L2 Patchouli retrieval 三层语义。迁移只替换 L1 cache 的依赖来源，并将 `IdentityScope` 传入 cache 查询所需的 Workspace 坐标；L0 仍由 PendingAtomRuntime 提供，L2 仍由 Patchouli 作为最终资源 owner。

`AgentProfileResolver` 仍负责调用 Patchouli profile route、错误映射和并发 miss 合并。WorkspaceRuntime 不直接调用 GlobalSystemBus，也不绕过 Patchouli 的 profile ownership 校验。

## 6. 跨系统契约变更

### 6.1 组合与依赖注入

- `SystemAssembler._build_runtime()` 新建并返回 `WorkspaceRuntime`；`WorkspaceAssetStore` 作为其子组件或显式 port 暴露。
- `AliceSystem` / `AliceRuntime` 接收 WorkspaceRuntime 提供的 profile/atom cache port，不再自行实例化两个 cache。
- `AgentProfileResolver`、`RuntimeAliasResolver` 的构造函数改为依赖抽象 port，避免 Alice import WorkspaceRuntime 的具体实现。
- `System` 停止流程保留 Patchouli drain、Alice stop、AssetStore close 的先后关系；WorkspaceRuntime 必须提供分阶段 shutdown，而不是提前清空仍被消费者使用的状态。

### 6.2 Cache API

profile cache 的 `get/store` 必须接受 WorkspaceIdentity 和 ActorIdentity；atom cache 的 alias 查询、写入和失效必须接受 WorkspaceIdentity。所有兼容入口在迁移完成前只允许由内部 adapter 使用，不能恢复无 scope 的公共读取。

### 6.3 MTP 与 Pending 契约

MTP 的公开 payload、`PendingAtomMaterializeTask`、`PendingAtomSettlement`、PendingAtom 事件和 `pending_alias` 格式不变。PendingAtom 的 Workspace 信息继续从 `RuntimeScope.identity_scope` 派生，并在 resolver 边界校验；本计划不增加 Workspace 复合 pending key。

## 7. 实施阶段

### WRT-0：冻结设计与兼容面（2026-09-13 完成）

- 明确 `WorkspaceRuntime`、profile cache port、atom cache port 的最小接口；
- 确认 `WorkspaceIdentity` / `ActorIdentity` 作为 cache key 组成，而不是使用 `IdentityScope` 中的关联 ID；
- 列出所有直接实例化 cache 的生产代码、测试 fixture 和导入路径；
- 为旧的“跨 Workspace 共享 profile/alias”测试标记待改行为，避免迁移过程中误把旧断言当作稳定契约。

#### WRT-0 实施记录

本节冻结后续阶段的接口与兼容面结论，实施以本节为准。

**1. Key 与可哈希性确认**

- profile cache key 冻结为 `(WorkspaceIdentity, ActorIdentity 投影, alias)`，其中 Actor 投影沿用既有 `AgentProfileCache.key` 语义，只取 `(user_id, agent_id, team_id)` 并剔除 `session_id`（`session_id` 是兼容字段，不作为资源身份，参与 key 会造成按会话的缓存碎片化）；atom cache alias 索引冻结为 `(WorkspaceIdentity, alias) -> UUID`，`UUID -> MemoryAtom` 保持全局索引。
- `WorkspaceIdentity` / `ActorIdentity` 均为 `frozen=True` 的 pydantic 模型，已验证按字段值生成稳定 `__hash__` 与值相等，可作为 tuple key 组成；其中 `ActorIdentity` 必须先做上述投影再入 key，不能整体入 key。
- 说明：当前 `IdentityScope` 模型（W0 后）已不携带 interaction/generation 等关联 ID，只由 `actor_identity + workspace_identity` 组成。WRT-0 条目中“不使用 `IdentityScope` 中的关联 ID”按现状理解为：key 由两个身份 value object 显式组成，不将整个 `IdentityScope` 对象或任何关联坐标作为 key。

**2. 最小接口冻结**

- `WorkspaceRuntime`（WRT-1 引入，`system/runtime/workspace/`）：进程级唯一聚合，内部创建且仅创建一份 `InMemoryWorkspaceAssetStore`、`AgentProfileCache`、`KoakumaAtomCache`；对外只暴露 `asset_store`、`profile_cache_port`、`atom_cache_port` 三个窄化入口与幂等的分阶段 `shutdown()`。`shutdown()` 只清理两个派生 cache，AssetStore 的 `close_and_clear()` 仍由 `HiveMemorySystem.stop()` 在 Alice/Patchouli stop 之后显式调用；cache 清理阶段的调用时点不得早于其消费者（Alice run 链路）停止，具体挂接位置在 WRT-1 结合 §8 生命周期验收确定。其中 `asset_store` 入口与 `shutdown()` 在 WRT-1 落地；`profile_cache_port`、`atom_cache_port` 及聚合内的两个 cache 成员随 WRT-2/WRT-3 迁移分阶段引入，WRT-1 不预留无消费者的空 port。
- `ProfileCachePort`（alice runtime 侧消费）：`get(workspace_identity, actor_identity, alias)` / `store(workspace_identity, actor_identity, alias, profile)`。
- `AtomCachePort`（agent runtime 侧消费）：`ingest_atom(s)(atom(s), *, workspace_identity)`、`get_atom_by_alias(alias, *, workspace_identity)`、`get_atom_by_uuid(uuid)`（UUID 全局读取保留）、`has_alias(alias, *, workspace_identity)`、`invalidate_alias(alias, *, workspace_identity)`、`clear()`、`size`。
- 两个 cache 的实现类保留在既有模块（`agent_runtime/aliases/cache.py`、`alice/runtime/profile_resolver.py`）原位演进 key 语义，仅创建与持有关系迁移到 WorkspaceRuntime；消费方一律依赖上述 port，不 import 具体实现。

**3. 直接实例化与消费点清单**

生产代码：

| 位置 | 角色 | 迁移阶段 |
|:---|:---|:---|
| `src/hivememory/alice/runtime/core.py`（`AliceRuntime.__init__` 构造 `KoakumaAtomCache`；`_refresh_l1_cache_for_settlement` 消费） | 直接构造 + 消费 | WRT-2 / WRT-4 |
| `src/hivememory/agent_runtime/aliases/cache.py`（`KoakumaAtomCache` 定义点） | 类定义 | WRT-2 原位演进 |
| `src/hivememory/alice/runtime/profile_resolver.py`（`AgentProfileCache` 定义；`AgentProfileResolver.__init__` 直接构造） | 直接构造 + 消费 | WRT-3 / WRT-4 |
| `src/hivememory/agent_runtime/aliases/resolver.py`（构造注入消费，L1 查询/回填 + `atom_cache` property） | 消费 | WRT-2 |
| `src/hivememory/agent_runtime/mtp/runtime.py`（`KoakumaRuntime.atom_cache` property；SEARCH 回填 `ingest_atoms`；UPDATE `invalidate_alias`） | 消费 | WRT-2 |
| `src/hivememory/alice/application/agent_run_service.py`（构造注入 `atom_cache`，预检索别名预热） | 消费 | WRT-2 |
| `src/hivememory/agent_runtime/aliases/__init__.py`（导出 `KoakumaAtomCache`） | 导入路径 | WRT-4 收敛 |

测试 fixture：

| 位置 | 角色 |
|:---|:---|
| `tests/unit/agent_runtime/aliases/test_cache.py` | 直接实例化（cache 行为单测） |
| `tests/unit/agent_runtime/aliases/test_resolver.py` | 直接实例化并注入 resolver |
| `tests/unit/agent_runtime/mtp/conftest.py`、`tests/unit/agent_runtime/mtp/syscalls/conftest.py`、`tests/integration/mtp/conftest.py` | 直接实例化并注入 `RuntimeAliasResolver` |
| `tests/integration/mtp/test_search_chain.py`、`test_read_chain.py`、`test_run_chain.py`、`test_koakuma_update_e2e.py` | 经 `koakuma.atom_cache` 预热与断言 |
| `tests/integration/alice/application/test_agent_run_service.py`、`tests/unit/alice/application/test_agent_run_service.py` | 经 runtime 注入与断言 |
| `tests/unit/system/contracts/test_public_routes.py` | 经 `system.runtime.atom_cache` 断言 settlement refresh |
| `tests/unit/alice/runtime/test_profile_resolver.py`、`tests/unit/core/test_agent_profile_fallback.py` | 直接构造 `AgentProfileResolver` |
| `tests/unit/alice/runtime/test_control_flow_refactor_baseline.py` | 断言 `AliceRuntime` 持有 `profile_resolver`（WRT-4 待改） |

**4. 已标记待改行为的旧契约测试**

- `tests/unit/agent_runtime/aliases/test_cache.py::test_same_alias_keeps_existing_global_cache_semantics`（WRT-2 反转）；
- `tests/unit/agent_runtime/aliases/test_resolver.py::test_l1_hit_revalidates_workspace_without_partitioning_cache`（授权重验部分保留，索引覆盖断言随 WRT-2 改写）；
- `tests/unit/alice/runtime/test_profile_resolver.py::test_same_actor_profile_cache_is_shared_across_workspaces`（WRT-3 反转）。

上述测试的 docstring 已显式引用本 Plan，迁移完成前不作为稳定契约。

### WRT-1：建立 WorkspaceRuntime 聚合（2026-09-13 完成）

- 在 `system/runtime/workspace/` 增加聚合类型和窄化 port；
- 将既有 WorkspaceAssetStore 接入聚合，但保留现有命令/读取接口；
- 在 SystemAssembler 中统一装配单实例；
- 增加对象图和停止顺序测试，证明不会产生第二个 Store 或每 Workspace Runtime。

#### WRT-1 实施记录

- `WorkspaceRuntime` 落地在 `system/runtime/workspace/runtime.py`：内部创建唯一 `InMemoryWorkspaceAssetStore`，暴露 `asset_store` 与幂等 `shutdown()`（锁内标志守卫）；两个 cache 的窄化 port 随 WRT-2/WRT-3 分阶段引入，本阶段不预留空 port。
- `_RuntimeBundle.workspace_asset_store` 字段替换为 `workspace_runtime`；Patchouli reader、WorkspaceAssetApplicationService、AttachmentParseService 三个注入点全部改为 `runtime.workspace_runtime.asset_store`，未新增任何 Store 创建路径。
- `HiveMemorySystem.stop()` 序列在 `gateway.stop` 之后、`workspace_asset_store.close_and_clear` 之前新增 `workspace_runtime.shutdown` 步骤（未 start 早退分支同样先 shutdown 再关 Store）；`shutdown()` 当前阶段不触碰 Store，WRT-2 接入 cache 清理。
- 测试：`tests/unit/system/runtime/workspace/test_runtime.py`（shutdown 幂等且不动 Store、实例间独立）；`tests/integration/system/test_workspace_asset_runtime.py` 新增组合根对象图（三个消费端同一 Store 实例）与 shutdown 停止顺序/失败不清理/重复 stop 幂等测试。

### WRT-2：迁移 KoakumaAtomCache（2026-09-13 完成）

- 实现 Workspace-aware alias index；
- 更新 `RuntimeAliasResolver`、AliceRuntime settlement refresh 和 AgentRunService 的注入路径；
- 保留 L1 命中后的 ownership/actor policy 重验；
- 覆盖同 alias 跨 Workspace、同 UUID 跨调用方、L2 回填和 invalidate 行为；
- WRT-1 遗留约束：接入 `shutdown()` 清理时保证清理动作幂等（或以 try/finally 约束标志语义），把 shutdown 日志升级为 info 并输出清理统计，同时回补“重复 stop 时 shutdown 幂等”的可观察断言。

#### WRT-2 实施记录

- `AtomCachePort`（`agent_runtime/aliases/ports.py`）：仅暴露生产消费者所需的 5 个方法（`ingest_atom(s)` / `get_atom_by_alias` / `get_atom_by_uuid` / `invalidate_alias`），`workspace_identity` 为 keyword-only；`has_alias`/`clear`/`size`/命中统计保留在具体类上，不进入窄化 port。
- `KoakumaAtomCache` 原位演进：alias 索引改为 `(WorkspaceIdentity, alias) -> UUID`，`UUID -> MemoryAtom` 保持全局；缺失或非 `WorkspaceIdentity` 的读写直接 `TypeError`，不存在无 scope 的公共读写；新增 alias 命中/未命中计数（§9.3 可观测统计，本 cache 无淘汰语义故无淘汰计数）；`invalidate_alias` 在其他分区别名仍指向同一 UUID 时保留原子条目，避免悬空索引（单引用场景行为与迁移前一致）。
- 所有权迁移：`WorkspaceRuntime` 创建并持有 `KoakumaAtomCache`，暴露 `atom_cache_port`；`shutdown()` 清空该 cache（先清理后置位，`clear()` 幂等），升级为 info 日志并输出清理统计；重复 shutdown 不重复清理，已有可观察断言。`AliceSystem` / `AliceRuntime` 新增必填 keyword-only `atom_cache` 参数，不再自行实例化。
- 注入路径：SystemAssembler 以 `runtime.workspace_runtime.atom_cache_port` 装配 Alice；`RuntimeAliasResolver`（L1 读取/回填/settlement canonical 查询）、`KoakumaRuntime`（SEARCH 回填、UPDATE 失效）、`AgentRunService`（预检索预热，携带 run context 的 Workspace 坐标）全部经 port 消费；Alice settlement refresh 的失效与回填均使用 PendingAtom 原始 `identity_scope` 的 Workspace 分区。
- 测试：WRT-0 标记的 `test_same_alias_keeps_existing_global_cache_semantics` 反转为 `test_same_alias_resolves_per_workspace`；原“无分区”resolver 测试拆分为分区回填与授权重验两条；新增同分区替换、共享 UUID invalidate、无 scope 拒绝、命中统计、shutdown 清理幂等与组合根 settlement 分区刷新断言（含 isolation 分区变体：刷新写入 PendingAtom 原始分区且默认分区无条目、L2 查询坐标断言）；新增组合根对象图测试（经聚合 port 写入、Alice 侧注入引用可读同一条目）；集成 mtp/Alice 测试的 cache 预热与断言全部携带显式 Workspace 坐标。

### WRT-3：迁移 AgentProfileCache（2026-09-13 完成）

- 将 key 改为 `(WorkspaceIdentity, ActorIdentity, alias)`；
- 更新 profile resolver 的并发 miss、缓存命中和测试 fixture；
- 增加同 Actor 跨 Workspace 同名 profile 不串扰、同 Workspace 跨 Actor policy 不串扰、Profile 缺失/越权错误不被缓存污染的测试；
- 评估 profile 更新后的失效策略。若本轮仍无事件/TTL，必须在限制中明确 stale 窗口。

#### WRT-3 实施记录

- `ProfileCachePort`（`alice/runtime/profile_resolver.py`）：仅 `get` / `store` 两个方法，按 `(workspace_identity, actor_identity, alias)` 授权坐标读写。`AgentProfileCache` 原位演进：key 为 `(WorkspaceIdentity, user_id, agent_id, team_id, alias)`——`ActorIdentity` 只取投影并剔除 `session_id`（WRT-0 冻结结论）；LRU 容量语义保持（默认 32），新增命中/未命中/淘汰统计；缺失或非身份对象的坐标直接 `TypeError`，不存在无 scope 的公共读写。
- 所有权迁移：`WorkspaceRuntime` 创建并持有 `AgentProfileCache`，暴露 `profile_cache_port`；`shutdown()` 幂等清空两个派生 cache 并输出统计。`AliceSystem` / `AliceRuntime` 新增必填 keyword-only `profile_cache` 参数，不再自行实例化。
- resolver 行为：`default`/`omni_doll`/空 alias 短路返回内置 profile 不变；并发 miss 合并保持单锁串行复查（key 含完整授权坐标，串行化只合并同坐标重复加载）；任何失败结果（缺失/越权/路由或系统故障）都在写入 cache 之前抛出，不污染缓存。
- **失效策略评估（§9.1）**：本轮维持 LRU、不引入 TTL 或 profile mutation invalidation 事件（对应非目标“不承诺 Profile 版本订阅”）。stale 窗口为条目在 LRU 中的驻留期，上限是进程生命周期（容量内最久未用条目可长期驻留）；已写入 cache docstring 已知限制，WRT-5 收尾时须随耐久性治理文档晋升为事实限制。
- 测试：WRT-0 标记的 `test_same_actor_profile_cache_is_shared_across_workspaces` 反转为 `test_same_actor_same_alias_caches_per_workspace`；新增同 Workspace 跨 team 分区、`session_id` 不碎片化、缺失/越权失败不缓存并恢复加载等 resolver 测试（原同 Workspace 跨 Actor 并发隔离测试保留）；新增 cache 本体单测（LRU 淘汰与统计、坐标拒绝、分区、clear）；组合根对象图断言 resolver 持有的即聚合唯一 cache 实例；shutdown 清理覆盖两个 cache。
- WRT-5 待办：晋升文档时在 boundaries 文档显式记录 `system.runtime.workspace → alice.runtime/agent_runtime.aliases` 的组合根向下依赖方向（实现类原位演进、聚合层引用具体实现的冻结决策）。

### WRT-4：收口旧所有权（2026-09-13 完成）

- 删除 AliceRuntime 对两个 cache 的直接构造和重复属性；
- 将生产代码、集成 fixture 和文档中的旧 import 收敛到 Workspace port；
- 运行完整 Alice、MTP、Workspace asset 和跨系统契约测试；
- 保留 PendingAtomRuntime 在 AliceRuntime 的显式依赖，避免通过聚合对象形成隐式所有权。

#### WRT-4 实施记录

- `AliceRuntime` 删除 `atom_cache` 公开属性与内部重复字段：cache 的唯一持有引用在 `RuntimeAliasResolver` 上，settlement refresh 经 `self._alias_resolver.atom_cache` 访问；`AgentRunService` 由 `AliceSystem` 直接传入注入的 atom cache port，不再经 runtime 属性中转。新增架构守卫测试固定"AliceRuntime 不暴露 atom_cache/profile_cache 属性"。
- import 收敛核验：`KoakumaAtomCache` / `AgentProfileCache` 具体类在生产代码中仅被 `system/runtime/workspace/runtime.py`（聚合）与包导出引用；全部消费方（resolver、KoakumaRuntime、AgentRunService、AliceRuntime）只依赖 `AtomCachePort` / `ProfileCachePort`。测试 fixture 按计划 §8 以显式构造具体类作为 port 替身，保留至归档。
- `PendingAtomRuntime` 仍由 `AliceRuntime` 显式创建、持有并注入 `RuntimeAliasResolver` 与 `AgentRuntime`，未通过聚合对象形成隐式所有权。
- 回归：完整 PR 快速集（unit + integration）2350 项全部通过；确定性 e2e（retrieval/generation component）在本环境因缺少 `qdrant` 配置无法初始化，已在基线提交上复现同一错误，属存量环境限制而非本次迁移回归（e2e pipeline 子集标记 live_llm，按运行条件排除）。

#### WRT-4 补充调整（用户裁决，2026-09-13）

应项目维护者要求，两个 cache 实现与 port 定义迁移至 workspace 包，统一依赖方向为"消费方从 Workspace 包导入端口与类型"：

- `KoakumaAtomCache` → `system/runtime/workspace/atom_cache.py`；`AgentProfileCache` → `system/runtime/workspace/profile_cache.py`；`AtomCachePort` / `ProfileCachePort` 并入 `system/runtime/workspace/ports.py`（与 WorkspaceAsset 端口同址）。
- 删除 `agent_runtime/aliases/cache.py` 与 `agent_runtime/aliases/ports.py`；`alice/runtime/profile_resolver.py` 只保留 `AgentProfileResolver`。聚合层不再反向引用 `agent_runtime`/`alice` 模块，全部消费方（resolver、KoakumaRuntime、AgentRunService、AliceRuntime/AliceSystem）从 `hivememory.system.runtime.workspace` 导入端口；已验证全部入口方向的导入无环。
- cache 行为单元测试随实现迁移：`tests/unit/system/runtime/workspace/test_atom_cache.py`、`test_profile_cache.py`（与被测模块同构）。
- 本调整取代 WRT-0 记录中"实现类保留在既有模块原位演进"的位置决策；key 语义、port 签名与所有权结论不变。WRT-5 晋升文档时以迁移后的实际位置为准。
- WRT-5 待办补充：清理指向已删除 `agent_runtime/aliases/cache.py` 的过期路径——`docs/governance/baselines/identity-s0-threat-model-inventory.md`（4 处链接）、`docs/governance/baselines/durability-d0-state-inventory.md`、`docs/todo/mtp-cache-scope-revalidation.md`（frontmatter code_paths）。

### WRT-5：事实文档晋升（2026-09-13 完成）

#### WRT-5 实施记录

- 新增 [ADR-0004](../../architecture/decisions/0004-workspace-derived-cache-partitioning.md) 记录分区例外决策；更新 Workspace 架构、System 组合根、Runtime and Bus、boundaries、Alice README/pending-atom/orchestration/mtp-runtime、MTP 契约与两份治理文档；修复 identity-s0/durability-d0 冻结基线与 mtp-cache-scope-revalidation Todo 中指向旧 cache 路径的断链；本 Plan 归档至 `docs/archive/plans/`，plans/README 与 ROADMAP 状态同步。

实现、测试、迁移和代码审查完成后，再更新当前事实文档并归档本 Plan。工作未收尾前，完整目标模型只保留在本 Plan；Roadmap 只保留计划摘要。

## 8. 测试与验收标准

| 范围 | 验收条件 |
|:---|:---|
| 组合根 | System 只创建一个 WorkspaceRuntime；其内部只创建一份 AssetStore、profile cache 和 atom cache |
| Profile key | 同 Actor + 同 alias 在不同 Workspace 可缓存不同 profile；同 scope 重复解析只加载一次 |
| Profile policy | 同 Workspace 不同 Actor 的 private/team profile 不因 cache hit 绕过 owner/policy；失败结果不进入 cache |
| Atom alias | 同 alias 在不同 Workspace 可同时命中各自 atom；同 Workspace 的 alias 替换不会留下旧 UUID 反查 |
| Atom authorization | L1 命中仍执行 Workspace ownership 和 actor policy 重验；越权命中不触发 READ/RUN/citation |
| L2 回填 | 冷查询按调用方 scope 回填正确 Workspace 条目，不污染其他 Workspace 的 alias 索引 |
| Settlement refresh | Pending settlement 刷新 L1 时使用 PendingAtom 原始 scope，不能写入当前调用方或默认 Workspace |
| Pending 保持 | PendingAtom 状态、settlement、claim、cancel、evict 和事件回归测试全部保持通过；其所有权仍为 Alice |
| 生命周期 | Alice/Patchouli drain 完成前，WorkspaceRuntime 不清空仍会被读取的 cache 或 AssetStore；重复 stop 幂等 |
| 兼容边界 | 无 scope 的 cache 公共读取不存在；旧 fixture 只能通过显式 adapter 构造，并最终删除 |

## 9. 风险与待决问题

1. **Profile stale**：Workspace 分区解决错误 Workspace 命中，但不自动解决 Profile 更新后的陈旧值。需要在实施阶段决定继续使用 LRU、增加 TTL，或接入明确的 profile mutation invalidation。
2. **MemoryAtom 可变引用**：atom cache 当前返回原始 `MemoryAtom` 引用。迁移不自动改变可变性；若后续需要深冻结，应另立数据模型治理任务。
3. **缓存容量**：按 Workspace 分区后，单一全局容量与每 Workspace 配额会产生选择。第一版保持现有容量语义，新增可观测的命中/淘汰统计，再依据真实负载调整。
4. **WorkspaceIdentity 作为 key**：WorkspaceIdentity 当前是 frozen value object，W0 仍要求 `workspace_key == workspace_id`。若未来身份模型改变，必须提供稳定的 cache key 投影，不能直接拼接可变字段。
5. **文档基线冲突**：当前事实文档仍声明共享 cache 不按 Workspace 分区。该冲突在实现收尾前保持显式；不得把本 Plan 的目标设计提前写入当前事实文档。
6. **PendingAtom 后续方向**：若未来引入 Workspace 级取消、配额、恢复或持久化 ledger，应另建 Plan，重新评估 PendingAtom 的资源归属和跨系统契约，不把本次 cache 迁移当作前置承诺。

## 10. 完成时需要更新的文档

- [Workspace 架构](../../architecture/workspace.md)：更新 WorkspaceRuntime 与两个 Workspace-aware cache 的实际边界；
- [系统组合与生命周期](../../system/composition.md)：补充 WorkspaceRuntime 的装配和分阶段关闭顺序；
- [System Runtime 与总线](../../system/runtime-and-bus.md)：说明该聚合不改变 GlobalSystemBus、队列、调度器和 EventBus 的共享语义；
- [Alice README](../../alice/README.md)、[Agent Runtime](../../alice/agent-runtime.md)：更新 cache 所有权、key 和依赖注入；
- [PendingAtom](../../alice/pending-atom.md)、[MTP 契约](../../contracts/mtp.md)：明确 PendingAtom 未随本次迁移进入 WorkspaceRuntime；
- [身份与执行安全治理](../../governance/security/identity-and-execution-safety.md)：更新 cache 命中校验和 Workspace 分区例外；
- [耐久性与故障恢复治理](../../governance/reliability/durability-and-recovery.md)：更新 cache 的可重建性、容量和 stale 限制；
- `docs/plans/README.md` 与 `docs/ROADMAP.md`：在计划完成后更新状态并归档本 Plan。

## 11. 完成出口

本计划只有在以下条件同时满足后才能标记为 `completed` 并归档：

1. WorkspaceRuntime、两个 cache port 和组合根装配已经落地；
2. profile/atom cache 的 Workspace-aware key、命中校验、失效和生命周期测试通过；
3. Alice、Patchouli、MTP、附件 Store 和 settlement refresh 的跨系统契约回归通过；
4. PendingAtomRuntime 仍由 Alice 持有，且没有新增隐式 Workspace 分区或第二份状态真相；
5. stale、容量、可变引用和重启丢失等限制已经记录；
6. 当前事实文档依据最终代码重写，旧的共享 cache 设计描述已归档或删除；
7. 代码审查不再存在会改变所有权、key 或失败语义的未决问题。
