---
title: v0.7.0 A2-P Memory Content Versions and Lifecycle State
status: planned
owner: patchouli
target: v0.7.0
scope: complete-memory-history-lifecycle-state-and-controlled-mutations
parent_plan: docs/plans/v0.7.0-workspace-resource-system-and-agent-execution-boundaries.md
depends_on:
  - docs/archive/plans/v0.7.0-a1-workspace-access-boundary.md
blocks:
  - docs/plans/v0.7.0-a2-workspace-resource-reads-and-caches.md
last_reviewed: 2026-09-22
updates:
  - docs/architecture/data-model.md
  - docs/patchouli/memory-library.md
  - docs/patchouli/lifecycle.md
  - docs/patchouli/generation.md
  - docs/patchouli/artifacts.md
  - docs/patchouli/memory-compiler.md
  - docs/contracts/subsystem-contracts.md
  - docs/system/configuration.md
---

# v0.7.0 A2-P：记忆内容版本与 Lifecycle 状态重构

本计划是 [A2 资源读取、Runtime 与派生缓存](./v0.7.0-a2-workspace-resource-reads-and-caches.md)的独立前置工作，简称 A2-P；不调整 A2–A6 的编号，也不与 A2 内部的 A2-0 冻结阶段混用。实施阶段使用 MVL 编号。本文唯一维护完整 MemoryAtom、内容版本、lifecycle 动态状态、历史记录与受控写入的目标契约；A2 消费这些成果并交付缓存协作。

目标版本仍为 v0.7.0。后续 v0.7.3 MTP READ 专项消费这里保存的历史数据，负责版本选择、片段、差异、引用与预算编译。不能等到编译器实现时才开始正确保存历史：此前丢失的嵌套内容无法从当前原子可靠补回。

本文是 Planned 设计，不表示代码已经采用结构化 provenance、嵌套 lifecycle、Memory schema `2.1`、新的 `agent_config` 位置或强制版本记录。当前调查依据为 2026-09-20 工作树；A1 已完成，A2-P 尚未实施。

## 1. 问题与当前证据

| 当前实现 | 证据入口 | 需要改变的行为 |
|:---|:---|:---|
| `meta.version` 描述为乐观锁，内容更新递增它，但没有对应的比较交换契约 | [Memory 模型](../../src/hivememory/core/models/memory.py)、[生成引擎](../../src/hivememory/engines/generation/engine.py)、[生成服务](../../src/hivememory/patchouli/services/memory_generation.py) | 固定为内容修订序号；不能承担所有字段的变化标记或并发锁 |
| 强化和批量评分修改 metadata 后整颗 upsert；引用还重置 `updated_at` | [Lifecycle](../../src/hivememory/engines/lifecycle/)、[中期 adapter](../../src/hivememory/patchouli/memory_library/adapters/mid_term.py) | 动态状态只经受限更新提交；内容修订时间与衰减基准分离 |
| 普通 upsert 重新编译 embedding 输入并计算向量 | [向量存储](../../src/hivememory/infrastructure/storage/vector_store.py) | 访问、反馈和评分不进入内容/向量重写路径 |
| `MemoryVersionSnapshot` 只保存正文与部分 Index 字段，遗漏 `agent_config`、Relations 等结构 | [Artifact 模型](../../src/hivememory/core/models/artifact.py) | 历史保存完整原子结构；撤回“现有裁剪记录支持完整重建”的假设 |
| `MemoryProvenance` 位于 generation 层，`MetaData` 与 Artifact 又各自平铺来源字段 | [生成模型](../../src/hivememory/engines/generation/models.py)、[Memory 模型](../../src/hivememory/core/models/memory.py)、[Artifact 模型](../../src/hivememory/core/models/artifact.py) | 将无生成依赖的 `MemoryProvenance` 上升至 `core.models`，由 `meta.provenance` 及相关 Artifact 统一复用；生成层只保留上下文相关的构造方法 |
| `history_summary` 仍在生成更新和 MemoryCompiler 中作为轻量 fallback | [生成引擎](../../src/hivememory/engines/generation/engine.py)、[MemoryCompiler](../../src/hivememory/engines/memory_compiler/) | 从新 Memory schema 删除；正式历史只由 `MemoryVersionArtifact` 承担，旧值迁移时报告其遗留和完整性 |
| `agent_config` 位于 `payload.artifacts`，但它是可更新的 Agent Profile 内容 | [AgentProfile](../../src/hivememory/core/models/agent.py)、[Memory 模型](../../src/hivememory/core/models/memory.py) | 移至 `payload.agent_config`；Artifact 聚合只保留 append-only 引用、事件和存储定位 |
| 版本 Artifact 可禁用；构建失败被捕获后，原子仍可写入 | [Artifact builder](../../src/hivememory/engines/artifacts/memory.py)、[生成服务](../../src/hivememory/patchouli/services/memory_generation.py) | 新内容提交成功必须有对应的完整版本记录，失败不能静默降级 |
| 新增 `MemorySnapshot` 为裁剪投影；现有编译器消费完整 MemoryAtom | [读取投影](../../src/hivememory/core/models/projections.py)、[MemoryCompiler](../../src/hivememory/engines/memory_compiler/) | 删除裁剪原子读取模型及导出，保留完整对象与引用隔离 |
| codec 只接纳 Memory schema v2，存储过滤和服务投影直接读取平铺 metadata | [codec](../../src/hivememory/engines/retrieval/memory_codec.py)、[MemoryLibrary](../../src/hivememory/patchouli/memory_library/)、[服务端投影](../../src/hivememory/server/models/memory.py) | 显式迁移 schema、过滤字段与消费者，不能只改 Pydantic 模型 |

问题根源是内容修订、使用状态、资源策略和存储迁移共享了过宽的写入路径。字段分组与写入边界必须一起修改；只移动字段无法防止旧对象覆盖新内容，也不能消除无意义的向量重算。

## 2. 目标、非目标与依赖

本计划交付：

1. 完整 MemoryAtom 与结构化 `meta.provenance`、`meta.lifecycle` 聚合；内容版本、内容时间、衰减基准各自独立。
2. 内容修订、动态状态更新、资源策略更新和存储迁移的受控写入边界，覆盖当前所有正式写入者。
3. 完整、不可变的 MemoryVersionArtifact 历史与可辨认的旧裁剪历史；新内容提交和历史记录的一致性。
4. Memory schema `2.1` 的迁移、结构化 provenance 和 `agent_config` 位置调整、`history_summary` 移除、现有读写消费者适配，以及交付 A2 的同步变更协作点。

不交付 WorkspaceRuntime/cache 迁移、Actor adapter 重排、Pending 状态机、MTP READ 新模式、历史 diff/预算编译、生命力算法重新调参、worth_saving 重设计、独立 lifecycle 数据库或通用事件溯源平台。归档/复活沿用 MemoryLibrary 责任，不扩展为长期记忆检索功能。

依赖 A1 已有授权边界和当前 MemoryLibrary/ArtifactStore，不等待 A2 缓存、A3 Session、A4 Pending、A5 API 目录、A6 生产装配或 B wire。独立组合真实 Patchouli 组件验收；已有消费者因字段变更所需的适配属于本计划，不以提前创建新 WorkspaceRuntime 或装配未稳定系统为条件。

## 3. 数据模型与不变量

### 3.1 完整原子与 Lifecycle 聚合

保留 MemoryAtom 的 `meta / index / payload / relations` 结构。在 MetaData 内持有两个结构化领域对象：`MemoryProvenance` 由 `meta.provenance` 持有，`MemoryLifecycleState` 由 `meta.lifecycle` 持有。不为每种更新另建投影或第二份原子模型。`MemoryProvenance` 定义在 `core.models`，不得由 core 反向依赖 generation；generation 只提供依赖 `GenerationContext` 的构造方法。

| 字段 | 目标位置与语义 |
|:---|:---|
| `schema_version` | MemoryAtom 顶层；新 canonical 格式为字符串 `"2.1"`，旧整数 `2` 只作为有期限的迁移输入；与 MemoryVersionArtifact 的 Artifact schema 独立 |
| `version` | `meta.version`；从 1 开始的内容修订序号，同一资源每次实际内容提交推进一次 |
| `created_at` / `updated_at` | 留在 meta；分别是创建时间与内容最后修订时间，访问、反馈、策略修改、schema 迁移不刷新它们 |
| `access_count` / `last_accessed_at` | 迁入 `meta.lifecycle`；沿用既有事件计数口径，不在本计划顺带改变 HIT/CITATION/FEEDBACK 含义 |
| `event_vitality_boost` | 迁入 `meta.lifecycle`；强化事件累计影响 |
| `vitality_score` | 迁入 `meta.lifecycle`；最近一次计算并保存的派生分数，不保证随时间自动保持实时值 |
| `decay_anchor_at` | 新增于 `meta.lifecycle`；遗忘衰减的时间基准，创建时取创建时间，主动复习和内容修订按既有/本计划规则推进 |
| `confidence_score` / `verification_status` | 迁入 `meta.lifecycle`；对当前内容的评估，可独立变化，不因评估变化创建内容版本 |
| `provenance` | `meta.provenance`；使用从 `core.models` 复用的 `MemoryProvenance`，记录 `source_agent_id`、`source_team_id` 和 `contributing_agent_ids`；来源事实不参与授权 |
| ownership / `access_policy` | 仍留在 meta 原职责位置，不混入 lifecycle；Workspace 归属和读取授权语义沿用 A1 |

既有数值范围、初始值和事件调整幅度保持现有规则。只有衰减时间源从 `updated_at` 改为 `decay_anchor_at`；时间输入统一为 UTC 并可在测试中控制。需要准确现时评分的排序或 GC 在决策时计算，不能把缓存中的物化分数误作任意时刻的实时结果。

不同操作仅能维护其负责的状态：评分计算只产生分数；HIT/普通访问维护计数和 `last_accessed_at`，但不推进 `decay_anchor_at`；CITATION 这种主动复习按既有规则推进 `last_accessed_at`、`decay_anchor_at` 和事件加成；内容修订按提交规则推进 `decay_anchor_at`；反馈/验证维护相应评估结果。聚合字段不授予任何调用方整体覆盖其他字段的权力。

### 3.2 内容版本覆盖范围

版本覆盖 Index、Payload 中的记忆正文与 `agent_config`、Relations 等内容事实。`history_summary` 从新 Memory schema 删除，正式历史只由 `MemoryVersionArtifact` 承担。实际内容相同的重复更新不创建新版本。alias、title、tags 等 Index 变化也属于内容修订。

`payload.agent_config` 是可更新的 Agent Profile 内容，变化要版本化；它不属于 embedding 输入，只有 Index 中实际参与编译的字段变化时才需要重算向量。`payload.artifacts` 只保留 append-only 的 Artifact 引用、生命周期事件和冷存储/复活定位；新增版本引用、CREATED/VERSIONED/ARCHIVED/REVIVED 事件以及存储定位维护不递归触发内容版本。MVL-0 按现有字段列出分类；后续新增字段必须指定类别，不允许因位于 Payload 或 Artifacts 就自动递增版本，也不能排除整个嵌套结构。

`meta.provenance` 是治理元数据，不因来源字段单独变化创建版本。`source_agent_id` 和 `source_team_id` 在创建时确定；`contributing_agent_ids` 只能随受控内容提交按既有规则合并，并与该次内容版本一同保存，不允许成为独立的整颗对象覆盖入口。

`MemoryCreationArtifact` 与 `MemoryVersionArtifact` 的来源字段也改为复用 `core.models.MemoryProvenance` 的结构化值，不在 Artifact 模型中继续维护另一组平铺 `source_agent_id`、`source_team_id` 和 `contributing_agent_ids`。Artifact 记录的 provenance 是写入时不可变的事实；它可以复制本次提交所使用的 `meta.provenance`，但不反向修改 canonical Memory，也不把 Artifact 的来源记录当作当前授权依据。

同一 `meta.version` 下 lifecycle、访问策略和运维引用可以变化，因此版本号不能比较整个 MemoryAtom 是否相等。AGENT_PROFILE 原子的解析结果可由缓存内部关联 source UUID 与内容版本，不新增配置版本，也不为此保留 `ProfileSnapshot.source_revision` 公共字段。`schema_version` 表达数据格式，cache epoch 表达运行时变更代次，三者不能互相替代。

### 3.3 引用隔离

删除 `core/models/projections.py` 中裁剪型 MemorySnapshot 及相关导出/说明。canonical 读取、缓存以及历史完整记录使用完整 MemoryAtom 或其完整结构序列化。隔离通过深拷贝和历史存储不可变性实现，不能摊平、挑选字段再拼回原子。

AGENT_PROFILE 同样保存完整 MemoryAtom；已有 `AgentProfile.from_atom()` 解析的是运行配置，不是第二份 canonical 资源，也不替代完整历史。公共 Profile 返回 AgentProfile，不保留 ProfileSnapshot 或替代包装；该模型、snapshot 路由及消费者的删除由 A2 的读取迁移负责，本计划不依赖其完成。这里保留 `payload.agent_config`、正文和来源，A2 再消费完整原子完成类型/可见性检查与解析缓存；源 UUID/version 仅需内部关联，builtin 无需伪造源原子。

修改调用方持有的 index、嵌套 `payload.agent_config`、relations、provenance、lifecycle 或 ArtifactRef 列表，不得影响缓存、canonical 状态或已保存历史。仅冻结外层 Pydantic 模型不足以证明嵌套对象已隔离。

## 4. 受控更新与职责分工

| 操作类别 | 负责边界 | 允许的变化 | 版本与向量行为 |
|:---|:---|:---|:---|
| 创建/内容修订 | Patchouli 现有生成、管理链，汇入唯一内容提交边界 | 内容字段、对应来源和历史关联；明确的内容更新后状态规则 | 创建 v1 或推进一次版本；保存完整历史；仅 embedding 输入变化时需要重算向量 |
| 生命周期维护 | Lifecycle 算法与服务，经 MemoryLibrary 受限状态更新 | 该业务操作允许的 lifecycle 字段 | 不改内容 version/updated_at，不生成版本 Artifact，不调用整原子 upsert 或 embedding |
| 资源策略修改 | 现有资源管理边界 | 访问策略 | 不制造内容版本；立即发布新的授权依据，不复用延迟统计刷新 |
| 归档/复活/删除 | MemoryLibrary 既有资源存储操作 | 所在层级、状态与运维引用 | 保留内容版本；物理迁移可保存完整原子，复活重建必要索引不等于内容修订 |

`VitalityCalculator` 保持纯计算职责。`refresh_vitality`、reinforce、批量维护以及 generation DISCARD 后的访问强化全部切换到受限状态更新；现有 `update_access_info` 调用点迁移到 `patch_payload` 后删除该专用入口。禁止状态维护拿到旧 MemoryAtom 后，通过通用 upsert 回写；也禁止把局部更新伪装成“加载旧对象再整颗保存”。存储 adapter 应使用真正的局部 payload 更新，保留原有向量和非目标字段。

不新增对外 lifecycle facade、Patchouli provider 或 Workspace 业务 service。继续沿 application → 现有领域服务/算法 → MemoryLibrary 的依赖方向修改。维护任务可使用已有内部可信资源键，Actor 操作仍通过 A1；内部维护不伪造 Actor 身份，也不因此开放无授权公共更新入口。

### 4.1 最小存储 mutation 接口

不为 lifecycle、访问、评分和资源策略分别新增专用 DTO 或存储方法。`MidTermStoragePort` 保留完整原子提交，并增加一个受限的局部 payload patch 原语；现有 `update_access_info()` 迁移为该原语的调用方，完成后从 Port 删除：

```python
async def upsert(
    self,
    memory: MemoryAtom,
    *,
    recompute_vectors: bool = True,
) -> None:
    """提交完整 canonical Memory；必要时重建 embedding。"""

async def patch_payload(
    self,
    key: WorkspaceMemoryKey,
    patch: Mapping[str, Any],
) -> MemoryAtom | None:
    """原子地更新允许的持久化字段并返回更新后的 Memory。"""
```

`patch` 使用 canonical dotted field path 到值的 mapping，由 adapter 映射为后端的局部 payload 更新；一次调用中的所有字段必须对同一资源一起提交。它不是公开的任意字段写入口，只允许：

- `meta.lifecycle.*`：生命周期状态的受控字段；
- `meta.access_policy`：资源读取策略的完整替换。

`meta.version`、`meta.created_at`、`meta.updated_at`、`meta.provenance`、`index.*`、`payload.content`、`payload.agent_config`、`payload.artifacts.*` 和 `relations.*` 不得通过 patch 更新。内容提交、来源变更、Artifact 引用/事件和关系变化都必须构造完整 MemoryAtom，经唯一内容提交边界使用 `upsert()` 完成。`patch_payload()` 不增加内容版本、不刷新内容时间、不写 MemoryVersionArtifact，也不重算向量；资源不存在时返回 `None`，存储错误直接传播。

`recompute_vectors=False` 只用于完整内容提交中 embedding 输入未变化的情况，例如仅修改 `payload.agent_config`；adapter 必须保留现有向量并替换完整 payload。CREATE、Index embedding 输入变化的内容 UPDATE 和从冷存储重新建立中期点时使用 `True`。生命周期、访问和策略路径必须使用 `patch_payload()`，不能以 `upsert(recompute_vectors=False)` 伪装局部更新。

`MidTermMemoryStore` 负责把同一 patch 转发给已配置的 secondary，并按既有 primary/secondary 失败策略传播或标记失败。调用方不新增 mutation result 模型，使用 `MemoryAtom | None` 和现有结构化存储错误。资源提交边界在调用 patch 或 upsert 前取得同一资源锁；Port 本身不保存业务锁、版本号或幂等结果。

### 4.2 并发与内容更新后的状态

首版保证当前支持的单进程写入者，按 Workspace + memory ID 协调所有受控更新；不宣称分布式写入或对绕过系统的直接数据库修改提供一致性。协调责任在资源提交边界，不在 Actor、不在 cache，也不通过给内容 version 增加乐观锁语义实现。

- 访问计数按最新状态施加增量；两个并发访问都必须保留。
- 批量评分在提交时依据有效的最新状态计算或重算；较早计算结果不能覆盖较新的事件强化。统计写入也不能让时间倒退。
- 内容候选只携带本次内容变更，提交时合并最新 lifecycle；不能把开始生成时的计数和分数整体带回。内容版本的分配与发布由唯一提交边界承担，算法不再提前自行递增后交给多个层重复保存。
- 内容修订继承累计访问与事件状态；由于原有内容更新会重启衰减，目标用推进 `decay_anchor_at` 保留该效果。生成 UPDATE 的置信度重置沿用已有明确规则，人工编辑与验证状态的继承/失效规则须在 MVL-0 逐项列出；只改变经该规则授权的字段，不顺带清空全部 lifecycle。
- 删除、归档与维护交错时，状态更新不能重建已删除原子或把已归档原子重新 upsert 回中期存储。

长任务的内容候选和新的内容提交发生交错时，MVL-0 必须明确沿用/收紧的内容冲突处理方式及串行范围；不得将保留最新 lifecycle 误称为已经解决语义内容合并。此决定只依赖现有生成/管理链，不等待 A4。

## 5. 完整历史记录

### 5.1 历史结构与捕获时点

MemoryVersionArtifact 保留 memory ID、版本号、来源、changelog 和证据引用等职责。新格式中的原子历史值直接保存完整 MemoryAtom 结构及其 schema，不再使用裁剪型 MemoryVersionSnapshot；可沿用 `snapshot_before/after` 字段名，但其值必须由统一完整原子 codec 编解码，不另建手工字段映射。Artifact 的 `schema_version` 是独立于 MemoryAtom 的版本轴：本计划目标为 MemoryVersionArtifact schema `"2"`，内嵌 MemoryAtom schema 为 `"2.1"`；`version_number` 仍表示该 Memory 的内容修订号，不能与任一 schema 版本混用。

捕获时点固定为：完成内容候选与最新 lifecycle 合并、确定版本及内容时间之后，追加本次版本记录自身的 refs/events 之前。此时的完整原子被复制并写入历史，随后 canonical 原子增加本次记录引用。这样不产生“记录包含自身 hash”的循环，也不是裁掉原子已有字段。记录已有 ArtifactRef，不递归内嵌它指向的所有 Artifact。历史恢复的是该捕获时点的完整对象，不声称与追加运维引用后的存储字节完全相同。

`snapshot_before` 若保留，来自受控提交时真实的修改前完整原子；`snapshot_after` 保存完整候选。v1 没有 before。嵌套配置、Relations、既有 refs/events 和生命周期均保留。写后不可变，后续统计或配置变化不能修改旧历史。

新 MemoryVersionArtifact 使用区别于旧裁剪格式的 schema（目标 `"2"`）；内嵌 MemoryAtom 使用自身 schema `"2.1"`。复用 ArtifactStore，不创建另一套历史仓库。MemoryAtom 与 Artifact 模型的循环类型引用通过现有模块拆分或完整序列化 codec 解决，不得因此退回 Any 字段任意丢数据或新建精简原子。

### 5.2 内容历史与状态时点

历史生命周期仅表示版本捕获时的状态，不是对每次访问的事件溯源。例如 v7 捕获时 access_count=10，当前 v7 已为 30，两份记录仍属于同一内容版本；状态维护不追加 v8，也不改写 v7 历史。

历史读取以相应版本保存的内容为依据。未来 READ 如附带当前评估或当前状态，必须标明时点，不能将当前置信度伪装成历史评估。历史原子中的旧访问策略只是一项历史事实；资源所有者根据当前有效权限授权历史访问，不得恢复旧策略获得读取许可。归档、删除后是否提供历史读取由后续 READ 契约明确，本计划不新增绕过当前授权的公开历史入口。

### 5.3 版本记录与内容提交一致性

新创建/内容修订的成功条件是：完整版本记录已持久化，canonical 原子已发布对应内容版本并关联该记录。`version_number` 与 `meta.version` 一致，记录与原子同属一个 Workspace 和 memory ID。版本历史不是可以在异常时省略的附加日志。

沿现有“先写 Artifact，再发布 canonical”的方向落实：先完成完整记录持久化，再发布带记录关联的 canonical，最后完成已配置的同步读侧协作并返回成功。两个存储并非同一数据库事务，不能把两个 await 声称为原子事务。

| 失败或中断位置 | 必须成立的结果 |
|:---|:---|
| 版本记录写入失败 | 不发布新 canonical 内容；不能捕获异常后返回无历史的成功 |
| Artifact 成功，canonical 确定未写入 | 旧 canonical 仍有效；未关联 Artifact 不能被当成已经提交的历史版本 |
| canonical 写入结果不明或提交被取消 | 核对持久化内容与记录关联后决定结果；无法核实时明确未决/不可用，不盲目重试并另增版本，也不假称已回滚 |
| canonical 已发布，同步读侧协作失败 | 保留真实提交事实，禁用可能过期的命中；由 A2 落实缓存恢复，不能再写一次内容以“补救”失效 |
| 重启后有孤立 Artifact | 历史有效性以 canonical 的已提交关联及记录一致性为依据；不能仅扫描目录取最大版本号作为 head |

重试同一次提交应复用可辨认的提交/Artifact 关联，避免为同一结果生成多个已提交版本；复用现有任务或业务提交信息，具体分配和核对方法在 MVL-0 冻结。只承诺本次资源写入的失败可判定与记录关联，不顺带承诺通用 queue 的跨重启可靠交付或分布式事务。

新的内容写入要求版本存储可用。现有 `NoOpMemoryArtifactBuilder` 和捕获异常后继续提交的分支不得再绕过这一条件：未配置或关闭版本存储时，内容 create/update 明确失败；已有资源读取和仅 lifecycle 更新仍可使用。其他可选来源 Artifact 的开关不应被误解释为允许跳过必需版本记录。配置兼容和启动诊断由本计划修改现有配置入口，不要求提前装配 A2 Runtime。

## 6. 交付给 A2 的变更协作契约

本计划在现有资源提交边界交付可组合的同步读侧协作点，表达资源键、变更类别与提交后的权威结果；无缓存配置时仍能独立工作。可以复用函数/现有组件接口，不为通知包装新增公共路由、provider、总 facade 或裁剪 DTO。异步 RuntimeEvent 只用于观测，不能承担提交正确性。

| 已提交变化 | 本计划保证 | A2 消费方式 |
|:---|:---|:---|
| 内容修订 | 内容版本、完整原子与完整历史一致 | 更新/失效 Atom、alias 及受影响的 Profile 解析结果 |
| lifecycle 更新 | 只更新允许状态，完整原子内容与版本保持；并发结果正确 | 同步刷新完整缓存原子的状态，Profile 解析输入不变则保留；不能可靠合并时失效 Atom |
| 资源策略更新 | 新 policy 生效且不制造内容版本 | 替换授权依据；源原子无效时解析结果不能单独放行 |
| 删除/归档/复活 | 操作结果及资源可读性明确，不制造内容修订 | 更新可读性与相关索引，阻止旧冷读恢复已失效资源 |

A2 负责运行时 epoch、在途冷读取、缓存失败时的禁用/恢复与 close；本计划不实现 cache，也不提前增加持久化 lifecycle revision。A2 不再重新定义生命周期字段、版本分配、历史格式或 schema 迁移，不把所有 metadata 变化一律处理成内容版本变更或 Profile 重解析。

## 7. 兼容与数据迁移

### 7.1 当前 Memory 数据

结构化 provenance、嵌套 lifecycle、`history_summary` 移除和 `agent_config` 位置调整共同构成持久化结构变化，目标 Memory schema 从旧的整数 `2` 升为字符串 `"2.1"`。先提供检查/迁移工具和可重复执行的转换，再切换正式写入；只有 `"2.1"` 成为唯一写入格式后才退出兼容。不能用新模型默认值吞掉旧字段，也不恢复已删除的 v1 隐式解释。Memory schema 与 MemoryVersionArtifact schema 独立演进；MemoryVersionArtifact 目标 schema `"2"` 不改变内嵌 Memory 的 `"2.1"`。

- 保持原有 ID、Workspace、内容 version、策略、Index、Payload 和 Relations；平铺 provenance 字段原值迁入 `meta.provenance`，平铺动态字段原值迁入 `meta.lifecycle`，不重新生成向量。
- 旧 `payload.artifacts.agent_config` 迁入 `payload.agent_config`；新旧位置同时存在且值不一致时拒绝，只有新位置进入正式写入。旧 `history_summary` 不写入新模型；迁移报告记录其存在、是否有对应完整版本 Artifact 以及无法恢复的历史范围。
- 旧 `updated_at` 可能已经被 CITATION 修改。以旧值初始化 `decay_anchor_at` 保持衰减行为；仅在存在可验证内容版本时间时恢复内容 `updated_at`，否则保留旧值并在迁移报告明确时间语义不确定，不能猜测历史内容时间。
- 元数据重排本身不增加内容版本、不生成伪造内容修改记录。活跃向量 payload、冷存储、备份/导出入口和已有服务消费者都纳入迁移核对。
- 同步调整过滤、排序、编译器、AgentProfile 解析、服务端响应投影和测试 fixture 对旧 metadata、`history_summary` 和 `agent_config` 路径的使用。现有外部响应中的平铺展示字段可以由 adapter 从结构化对象映射，不能因此保留两份可写的领域状态。旧过滤参数是否暂时映射须列入 MVL-0 兼容表。
- 新旧字段并存且值冲突、未知 schema、跨 Workspace 数据或 provenance/agent_config 位置冲突必须明确拒绝；不能按字段存在顺序任选。Memory schema `2` 的兼容解码只作为有期限的迁移入口，不允许长期双写。

迁移流程包括只读预检、备份、暂停受影响写入、转换、校验、恢复写入与报告。回滚先恢复与旧程序匹配的数据备份；不能让旧 codec 直接读取新 schema，更不能删除新增历史。迁移工具需可断点核对和安全重跑，记录已转换/失败/无法完整恢复的范围。

### 7.2 旧历史与当前基线

旧 MemoryVersionArtifact schema `"1"` 保留为明确的裁剪历史。专用解码按 Artifact schema 披露完整性；不把缺失配置/Relations/`agent_config` 填成默认值后声称完整，不使用当前原子补造旧版本，也不原地改写 append-only 历史记录。旧记录中的 `MemoryVersionSnapshot` 不是 Memory schema `"2.1"`，不得把它升级标记后伪装成完整原子。旧 MemoryCreationArtifact/MemoryVersionArtifact 的平铺 provenance 字段在受控读取或迁移时规范化为 `MemoryProvenance`；新 Artifact schema `"2"` 只写结构化 provenance。

已有当前原子没有完整版本记录时，可在受控迁移时为其当前版本保存完整基线记录，明确捕获时间和迁移来源。它证明“迁移时当前版本的完整内容”，不冒充最初提交时状态，不递增内容版本，也不覆盖同版本既有裁剪证据。基线标识及与旧记录的选择规则在 MVL-0 冻结；不能以任意一个同版本 Artifact 取代明确关联。

后续内容修改从该基线继续保存完整新版本。旧缺口进入迁移报告并交给 READ 显式表达“不完整/缺失”，不阻止未来记录正确建立。删除的旧内容如无完整证据，不能承诺恢复。

## 8. 实施切片与代码落点

| 阶段 | 产出 | 完成门槛 |
|:---|:---|:---|
| MVL-0 边界冻结 | 逐字段 mutation 分类、当前全部写入者清单、内容更新后的评估规则、提交/重试关联、schema 与旧历史基线兼容表 | 未决项全部由本计划关闭；没有等待 A2/A4/READ 设计才能确定的持久化语义 |
| MVL-1 模型与 codec | `core.models.MemoryProvenance`、MemoryLifecycleState、Memory schema `"2.1"`、完整版本记录格式、裁剪 MemorySnapshot 删除、`history_summary` 移除、`agent_config` 移位、现有消费者适配 | 完整嵌套字段往返不丢失；旧裁剪历史不能解码成伪完整原子；迁移工具预检可用 |
| MVL-2 受控 mutation | `upsert(recompute_vectors=...)`、受限 `patch_payload()`、移除 `update_access_info()`、内容提交唯一版本分配、按资源键并发协调、归档/复活/删除协作 | 维护不触发内容重写/embedding/version Artifact，内容和动态状态互不覆盖；局部 patch 不越过字段白名单 |
| MVL-3 历史与提交 | 完整历史构建、必需版本存储、失败/取消/重试核对、同步协作点 | 无历史的新内容不能成功；孤立记录不是已提交版本；无缓存、无 Alice 可独立验证 |
| MVL-4 迁移与交付 | 真实格式迁移证据、行为测试、实现审查、A2 交接清单 | 所有出口验收完成后 A2 开始依赖新 mutation/schema；READ 得到稳定历史数据入口 |

主要改动位置：

| 范围 | 现有入口与调整 |
|:---|:---|
| 领域模型与投影 | `core/models/provenance.py`（或等价 core 模块）、`core/models/memory.py`、`agent.py`、`artifact.py`、`projections.py` 及导出；Workspace 的投影说明同步移除裁剪原子约定 |
| 编解码与存储 | `engines/retrieval/memory_codec.py`、`infrastructure/storage/vector_store.py`、`patchouli/memory_library/{ports,stores,library}.py` 与 mid/long/artifact adapters；新增局部 payload patch 并验证向量保留 |
| 生命周期 | `engines/lifecycle/{vitality,reinforcement,engine,garbage_collector}.py`、`patchouli/services/lifecycle.py`；整原子写回退出维护路径 |
| 内容与历史 | `engines/generation/{engine,models}.py`、`engines/artifacts/{memory,engine}.py`、`patchouli/services/memory_generation.py`；来源构造改为复用 core `MemoryProvenance`，删除 `history_summary` 写入，收敛真实提交权威 |
| 读取消费者与配置 | Retrieval 过滤、MemoryCompiler、Profile/Memory 管理、server response、Artifact 配置；适配 `payload.agent_config` 和独立 schema 轴，不提前迁移 Actor 链路 |

## 9. 测试与验收

遵循[测试设计规范](../governance/testing/test-design-standards.md)。测试保护持久化、并发、版本和权限边界，不以字段赋值、类名或目录数量作为证据。时钟可控；并发使用事件屏障和有界等待，不以 sleep 猜测时序。

| 场景 | 必须观测的结果 |
|:---|:---|
| HIT、CITATION、正负反馈、评分刷新与批量 GC 前评分 | 正确动态状态落库；内容、version、内容时间和历史记录不变；无需可用 embedding 即可完成状态更新 |
| 两个访问并发、旧评分晚到、内容修订与反馈交错 | 不丢增量、不倒退状态、不覆盖新正文；只对内容修订生成一个新版本 |
| 无变化更新、agent_config/Relations/Index 修改 | 无变化不增版本；真实内容变化生成完整可重建的新版本，嵌套配置保留 |
| 历史记录后继续使用记忆或修改返回对象 | 历史完整值不变；当前状态正常推进；嵌套对象无共享引用污染 |
| Artifact 失败、canonical 写入失败/结果不明、取消和重试 | 成功/未决/失败与实际持久化一致，无静默缺历史或重复已提交版本；孤立记录不成为 head |
| 归档/复活/删除与状态维护并发 | 内容版本保持；完整结构跨层保存，维护不复活已删/归档对象 |
| Memory schema `2`→`"2.1"` 迁移、旧裁剪历史与未知格式 | provenance 结构化、`agent_config` 移位、`history_summary` 遗留可辨认；当前内容及向量不变，完整/裁剪/缺失可区分；冲突输入拒绝，无猜测补全；重跑不产生重复基线 |
| 两个 Workspace 中的资源及 Artifact 引用 | 版本、状态与提交关联不串扰；历史旧 policy 不成为新授权依据 |
| 局部 mutation 接口 | lifecycle/策略 patch 只改变白名单字段，不增加内容版本、不改向量；`upsert(recompute_vectors=False)` 保留向量；secondary 协作遵循统一失败策略 |

unit 覆盖确定性状态规则、schema/完整性判断与单一提交行为；integration 使用真实 generation/lifecycle、MemoryLibrary、Artifact builder/codec 和临时文件存储验证协作。外部模型可替换，不能 mock 掉声称被验证的内部提交链。实际 Qdrant 局部更新与向量保留需 `integration + real_infra` 证据；未运行时明确作为待验收项，不能用内存 store 宣称数据库契约已验证。不要求 live LLM 才能验证本次重构。

A2-P 独立验收不依赖缓存存在：验证真实资源写入和同步协作点，A2 再验证命中状态更新、Profile 保留及 epoch 防旧回填。实际运行命令在实现时按 `pyproject.toml` 与 CI 配置选取，不预写未经核对的测试命令。

## 10. 风险、交接与收口

主要风险是跨存储提交并非事务、旧历史不可恢复、字段迁移漏掉过滤/冷存储消费者，以及状态与内容并发覆盖。MVL-0 需要冻结的具体实现决定为：内容冲突串行范围、各内容更新操作的置信度/验证状态规则、提交重试关联、迁移基线标识和旧参数兼容窗口；其结果写回本文，不留给 A2 或 READ 反向决定。

交付 A2 的清单必须包含新 schema/完整原子样例（含 `payload.agent_config`）、`upsert(recompute_vectors=...)` 与受限 `patch_payload()` 的正式入口、patch 白名单和向量保留保证、同步读侧协作契约、失败后可读性、当前写入者覆盖与迁移结果。A2 消费这些数据完成公共读取和缓存维护，不重新开放 MemorySnapshot/ProfileSnapshot、内容 version 乐观锁或 lifecycle 整原子 upsert 的选项；列表/AgentProfile 返回与旧 envelope 迁移由其独立验收。

交付后续 READ 的清单包括完整版本定位与有效关联、历史原子解码、旧裁剪/缺失辨认、捕获时间语义和当前资源授权要求。历史编译与协议呈现后置，可靠历史的保存和读取基础不能后置。

仅在实现、必要数据迁移、测试、审查和本计划验收完成后，按[文档治理](../DOCUMENTATION.md)更新 frontmatter 中列出的事实文档，将本计划归档，并同步 A2、计划协调入口、plans/README 与 ROADMAP。本次制定计划不修改当前架构事实、ADR-0004 状态或 A1 的已完成结论。
